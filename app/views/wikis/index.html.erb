<h1>Wikis</h1>
<% if current_user %>
  <div class="col-md-4">
    <%= link_to "New Wiki", new_wiki_path, class: 'btn btn-success' %>
  
    <% @wikis.each do |wiki| %>
        <p><%= link_to wiki.title, wiki_path(wiki.id) %></p>
    <% end %>
</div>
<% end %>