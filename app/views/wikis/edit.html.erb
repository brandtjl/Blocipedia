<h1>Edit Wiki</h1>
<% if current_user %>
    
    <div>
        Collaborators: <br>
        <% @wiki.collaborators.each do |c| %>
            <% user = User.find(c.user_id) %>
            <p><%= user.email %></p>
            <%= link_to "Delete", "/collaborator/#{collaborator.id}", method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this wiki?'} %>
    </div>
    <div>
            <%= form_for new_collaborator_path, :url => {:action => "new", :id => @collaborator} do |f| %>
                <p><%= f.text_field :email, class: 'form-control' %>
                <% f.hidden_field :wiki_id, :value => @wiki_id %>
                <% f.hidden_field :user_id, :value => @user_id %>
                <%= f.submit 'Add Collaborator' %>
            <% end %>
        <% end %>
    </div>
<% end %>
<div class="col-md-8">
     <%= render partial: 'form', locals: { wiki: @wiki } %>
</div>